# Markdown 速查表（VS Code 版）

> 我（Kai）2026-09-11 整理。日常写笔记够用了，不用背，用的时候回来翻。

---

## 一、最常用的 8 个快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl + Shift + V` | 打开预览（新标签页） |
| `Ctrl + K` 松开再按 `V` | 左侧写、右侧预览（最好用） |
| `Ctrl + B` | **加粗**（只在 md 文件里有效；别的文件里是隐藏侧边栏） |
| `Ctrl + I` | *斜体* |
| `Ctrl + Enter` | 光标下方**新起一行**（在表格里就是新增一行，很好用） |
| `Shift + Alt + ↓` | 把当前行**复制一份**到下面 |
| `Alt + ↑ / ↓` | 把当前行**上下移动** |
| `Ctrl + Shift + K` | 删除当前行 |

## 二、编辑提效（不限于 md）

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl + D` | 选中下一个**相同内容**，连着按可以多光标批量改 |
| `Ctrl + Shift + L` | 一次选中**所有**相同内容 |
| `Alt + 鼠标点击` | 手动加一个光标，多点同时编辑 |
| `Ctrl + /` | 注释 / 取消注释 |
| `Ctrl + Shift + O` | 跳转到标题（md 文件里就是大纲导航，长文档神器） |
| `Ctrl + P` | 按文件名快速打开 |
| `Ctrl + Shift + P` | 命令面板（快捷键忘了就在这搜） |
| `Ctrl + \` | 拆分编辑器，左右对照 |
| `Ctrl + Shift + E / F / G / X` | 资源管理器 / 全局搜索 / 源代码管理 / 扩展 |
| `Ctrl + \`` | 打开终端 |
| `Ctrl + Shift + Z` | Zen 禅模式，全屏写作，`Esc Esc` 退出 |
| `Tab` / `Shift + Tab` | 列表**升级 / 降级**（做子节点） |

## 三、语法里的坑（新手最常踩）

**1. 井号后面必须有空格，否则不生效**

```markdown
#标题        ← 不会渲染成标题，就是普通文字
# 标题       ← 正确
```

**2. 列表符号后面也要有空格**：`- 内容`，不是 `-内容`。

**3. 换行有两种**
- 行尾打**两个空格**再回车 → 强制换行
- 直接**空一整行** → 新起一个段落（推荐，更规范）

**4. 特殊符号要转义**：`\*` 显示 `*`，`\_` 显示 `_`。

**5. 代码块的三反引号要写语言名**，GitHub 上才有高亮：

````markdown
```c
printf("hello\n");
```
````

## 四、常用语法速填

```markdown
# 一级标题（一份文档只用一个）
## 二级标题
### 三级标题

**加粗**   *斜体*   ~~删除线~~   `行内代码`

- 无序列表
  - 缩进两格或 Tab = 子项

1. 有序列表
2. 第二项

> 引用一段话，可以套 `>` 变多层

[链接文字](https://github.com/TXDYLZF)

![图片说明](assets/图1.png)

- [ ] 待办事项（GitHub 上能点勾）
- [x] 已完成的事

| 左对齐 | 居中 | 右对齐 |
| :--- | :---: | ---: |
| a | b | c |

---   ← 分割线
```

## 五、两个"不用敲"的小魔法

- **链接**：先选中一段文字，再 `Ctrl + V` 粘贴网址 → 自动变成 `[选中文字](网址)`。
- **图片**：把图片文件从资源管理器**直接拖进编辑器** → 自动插入 `![](相对路径)`。先在仓库里建个 `assets` 文件夹，把图丢进去，再写路径，这样推到 GitHub 别人也看得到。

## 六、VS Code 里值得装的两个扩展

1. **Markdown All in One** —— 表格按 `Alt + Shift + F` 自动对齐；命令面板搜 `Create Table of Contents` 一键生成目录；列表自动编号。
2. **Markdown Preview Enhanced** —— 导出 PDF / HTML / 图片；支持 mermaid 流程图，写实验报告好用。

> 扩展别贪多，装两个就够。装多了 VS Code 启动会变慢。

## 七、写笔记的几条实用习惯

1. **文件名用连字符不用空格**：`03-实型与浮点.md`，比 `03 实型 与浮点.md` 更稳，不容易出乱码。
2. **一份笔记一个主题**，别把所有内容堆在一个大文件里。
3. **标题层级别跳级**：有 `##` 才用 `###`，不然大纲面板会乱。
4. **代码块里写清楚语言**（`c` / `bash` / `makefile`），既能高亮，也相当于标注了环境。
5. **写完顺手提交**：源代码管理面板填一句说明 → 提交 → 同步更改，别攒着。
